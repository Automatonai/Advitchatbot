version: "3.9"

services:
  on-prem-llm-advitchatbot:
    build: .
    container_name: on-prem-llm-advitchatbot
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    restart: unless-stopped
    env_file:
      - .env
